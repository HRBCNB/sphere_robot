# 球形机器人跳跃避障规划中期展示流程

本文档用于中期报告现场展示。展示重点不是单纯跑通程序，而是讲清楚：为什么需要在传统路径规划里加入球形机器人的滚动/跳跃语义，目前已经实现了哪些模块，以及 RViz 中应该观察哪些效果

## 一、展示主线。

建议按下面这条逻辑展开：

1. 球形机器人运动特点：既可以在地面滚动，也可以对低矮障碍进行跳跃越障。
2. 普通 A* 或普通 B-spline 轨迹无法区分“可滚越、可跳越、必须绕行”的障碍语义。
3. 当前工作在 EGO-Planner 框架上加入了 ROLL / JUMP 混合 A*，并把 A* 结果转换为 B-spline 轨迹。
4. 中期展示采用完整已知静态地图，不展示在线重规划；开局给定一个固定终点，规划器一次性生成可执行轨迹。
5. 通过 RViz 和曲线展示：低障碍滚越、低墙跳跃、高障碍绕行、B-spline 轨迹发布以及固定轨迹跟踪效果。

## 二、系统框架梳理

中期报告中可以从 EGO-Planner 的状态机开始讲，再逐层展开到 A*、B-spline 和可视化。

### 1. 状态机初始化与目标触发

系统启动后，规划节点首先进入 FSM 状态机。初始状态为 `INIT`，等待里程计、地图和目标点等信息准备完成。

状态流转大致为：

```text
INIT
  -> WAIT_TARGET
  -> GEN_NEW_TRAJ
  -> EXEC_TRAJ
  -> 保持执行固定轨迹
```

各状态含义：

- `INIT`：等待系统初始化完成
- `WAIT_TARGET`：等待目标点
- `GEN_NEW_TRAJ`：收到目标点后生成第一条轨迹
- `EXEC_TRAJ`：执行当前轨迹
- `REPLAN_TRAJ`：EGO 原始在线框架中的重规划状态，中期展示不使用

中期展示固定设置 `enable_local_replan=false`、`enable_periodic_replan=false`。系统只在开局收到目标后进入一次 `GEN_NEW_TRAJ`，规划成功后进入 `EXEC_TRAJ`，随后执行同一条轨迹，不再反复从 `EXEC_TRAJ` 跳回 `REPLAN_TRAJ`。

### 2. 固定终点与一次性规划范围

中期展示不讲在线滚动重规划，而是把问题设定为：机器人开局已经获得完整静态地图，用户只给一个固定终点，规划器一次性生成从起点到终点的轨迹。

目标点由 launch 中的固定目标发布节点给出：

```text
goal_x, goal_y, goal_z
```

为了保证“一次规划到终点”，中期演示使用 `midterm_global_demo.launch`。该场景是一张稍大的已知静态地图，终点放在一次搜索范围内：

```text
起点 (-18.0, 0.0, 0.0) -> 终点 (0.0, 0.0, 0.0)
planning_horizon = 18.5
astar_pool_size = 240 x 140 x 50
```

这段距离约 18 m，覆盖滚越、跳跃、绕行、二次跳跃和窄通道视觉障碍。这里不是在线重规划，而是通过增大 A* 搜索池支持一次性固定轨迹搜索。

### 3. 已知静态地图输入

地图输入链路为：

```text
/map_generator/global_cloud
  -> pcl_render_node
  -> /pcl_render_node/cloud
  -> /grid_map/cloud
  -> GridMap
```

中期展示按“完整地图已知”的设定讲。`midterm_global_demo.launch` 将 `sensing_horizon`、`local_update_range_x` 和 `max_ray_length` 调大到约 22 m，使起点到终点之间的展示障碍在开局就进入 `grid_map`，因此轨迹生成不依赖后续重规划逐步发现障碍。

### 4. PlannerManager 调度规划流程

FSM 不直接执行 A* 或 B-spline，而是调用 `EGOPlannerManager` 统一调度。

整体流程为：

```text
FSM 触发规划
  -> EGOPlannerManager 调度
  -> ROLL/JUMP 混合 A* 搜索
  -> 得到带运动模式的离散路径
  -> 转换为 B-spline 连续轨迹
  -> 最终碰撞检查
  -> 发布轨迹和 RViz 可视化
```

可以把 `PlannerManager` 作为系统中的核心调度模块来介绍。

### 5. ROLL / JUMP 混合 A* 搜索

传统 A* 主要判断当前位置是否可通行。当前工作在 A* 中加入球形机器人的运动语义：

```text
ROLL：普通滚动
ROLL_OVER：低障碍滚越
JUMP：跳跃越障
DETOUR：高障碍绕行
```

基本判定逻辑为：

```text
障碍高度 <= roll_over_height
  -> ROLL_OVER

roll_over_height < 障碍高度 <= max_jump_h
  -> 尝试 JUMP

障碍高度 > max_jump_h
  -> DETOUR
```

JUMP 还需要满足：

- 跳跃距离不超过 `max_jump_d`
- 起跳前保留 `jump_takeoff_clearance`
- 落点后保留 `jump_landing_clearance`
- 抛物线高于障碍顶部 `jump_vertical_clearance`
- 跳跃方向满足 `frontal_jump_cos`

这一部分是当前工作的核心算法模块。

### 6. A* 代价函数设计

混合 A* 仍采用标准 A* 的评价形式：

```text
f(n) = g(n) + h(n)
```

其中：

- `g(n)`：从起点到当前节点的累计代价
- `h(n)`：当前节点到局部目标点的启发式估计代价
- `f(n)`：节点优先级，`f(n)` 越小越优先扩展

启发函数采用对角距离启发，并加入很小的 tie breaker：

```text
h(n) = tie_breaker * h_diag(n, goal)
tie_breaker = 1 + 1 / 10000
```

对角启发函数 `h_diag` 根据栅格索引差值计算，相比曼哈顿距离更适合八邻域/三维邻域搜索，可以让 A* 更倾向沿对角方向接近目标。

#### 6.1 普通 ROLL 代价

普通空闲栅格扩展时，代价由步长和偏离主线代价组成：

```text
cost_roll = step_cost + cost_line_deviation
```

其中：

```text
step_cost = sqrt(dx^2 + dy^2 + dz^2)
```

当前滚动搜索中 `dz = 0`，所以主要是在水平平面内扩展。直走代价为 `1`，斜向走代价为 `sqrt(2)`。

偏离主线代价为：

```text
cost_line_deviation = line_deviation_weight * d_line^2
```

`d_line` 表示当前点到“起点-局部目标点”连线的横向距离。这个代价用于避免路径在没有必要时过度绕远。

#### 6.2 ROLL_OVER 低障碍滚越代价

当障碍高度满足：

```text
obstacle_height <= astar_height + roll_over_height
```

该障碍允许滚越，但会增加额外代价：

```text
cost_roll_over = step_cost + roll_over_penalty + cost_line_deviation + cost_jumpable_detour
```

其中 `roll_over_penalty` 表示滚越低障碍相对普通滚动的额外代价。它的作用是：低障碍可以通过，但如果旁边有代价更低的平坦路径，规划器仍可以选择绕开低障碍。

#### 6.3 JUMP 跳跃代价

当障碍高于滚越阈值、低于最大可跳高度，并且通过跳跃可行性检查后，A* 会生成 JUMP successor。

跳跃代价为：

```text
cost_jump = jump_distance + jump_penalty + cost_line_deviation(landing_pos)
```

其中：

- `jump_distance`：从当前点到落点的水平/空间距离
- `jump_penalty`：选择跳跃的固定额外代价
- `cost_line_deviation(landing_pos)`：落点偏离主线的代价

`jump_penalty` 用于避免规划器过度偏好跳跃。也就是说，只有当跳跃比绕行更合理，或者障碍确实适合跳过时，A* 才会选择 JUMP。

#### 6.4 提前起跳代价

为了避免机器人走到障碍边缘才起跳，当前在普通 free cell 扩展时会向前扫描可跳障碍。如果前方短距离内发现可跳障碍，就提前生成 JUMP successor。

提前起跳的代价形式与普通 JUMP 一致：

```text
cost_early_jump = jump_distance + jump_penalty + cost_line_deviation(landing_pos)
```

区别在于落点最小距离按：

```text
obstacle_rear_distance + jump_landing_clearance
```

计算，保证落点在障碍后沿之后，而不是刚越过障碍就落地。

#### 6.5 绕行相关代价

如果前方存在可跳障碍，但当前扩展仍选择 ROLL 绕行，则会加入额外绕行代价：

```text
cost_jumpable_detour = jumpable_detour_penalty
```

它的作用是：当障碍适合跳跃时，适当惩罚绕行路径，使规划器更倾向选择短跳；但如果跳跃不可行或障碍太高，系统仍会进入 detour 绕行。

#### 6.6 总体代价含义

因此，当前混合 A* 的代价函数不是单纯最短路径，而是综合考虑：

- 路径长度
- 偏离主线程度
- 低障碍滚越代价
- 跳跃动作代价
- 可跳障碍绕行惩罚
- 跳跃可行性约束

可以概括为：

```text
minimize: 路径长度 + 模式切换代价 + 主线偏离代价
subject to: 滚越高度约束、跳跃距离约束、跳跃高度约束、起跳/落点安全约束、碰撞约束
```

通过这种代价设计，规划器不会简单地“能跳就跳”，而是在 ROLL、ROLL_OVER、JUMP 和 DETOUR 之间做代价权衡。

### 7. A* 到 B-spline 连续轨迹

A* 输出的是离散路径点，不能直接作为连续轨迹执行，因此需要转换为 B-spline。

当前处理方式：

- ROLL 段允许轻量平滑
- JUMP 段尽量保留 A* 生成的抛物线形状
- 急转弯和跳跃段增加锚点约束
- direct A* 默认跳过旧 rebound optimizer，避免破坏 ROLL/JUMP 语义

这样做的目的，是避免普通 B-spline 把 JUMP 抛物线拉平或切入障碍，使最终蓝线和 A* 路径语义保持一致。

### 8. 最终碰撞检查

B-spline 生成后不会直接发布，而是先进行最终碰撞检查。

当前检查逻辑为：

- 进入 raw 实体障碍：拒绝
- ROLL 进入不可滚越障碍：拒绝
- JUMP 穿过 raw 实体障碍：拒绝
- JUMP 越过可跳低障碍的 inflated shell：允许
- ROLL_OVER 通过低障碍：允许

如果失败，终端会出现：

```text
final B-spline hits inflated obstacle, reject traj
```

这说明候选轨迹没有通过最终检查，RViz 中通常表现为只有失败候选轨迹，没有最终蓝线。

### 9. 轨迹发布与 RViz 可视化

最终通过检查的轨迹会发布给 `traj_server`，同时在 RViz 中显示。

可视化含义：

- 洋红色点：A* ROLL 点
- 橙红色点：A* JUMP 点
- 蓝色线：最终通过检查的 B-spline
- 橙红色线：被拒绝的候选 B-spline
- 青绿色球：目标点

这一部分可以作为实验验证平台来介绍，不只是“显示颜色”，而是为了区分离散决策、连续轨迹和失败候选轨迹。

### 10. 总体框架表述

汇报中可以用下面这句话概括：

```text
系统以 EGO-Planner 的 FSM 为调度核心，在开局获得完整静态地图和固定终点后构建一次性规划任务；随后基于栅格地图执行 ROLL/JUMP 混合 A* 搜索，根据障碍高度和跳跃约束生成带运动模式的离散路径；再将离散路径转换为保持跳跃语义的 B-spline 连续轨迹，并经过最终碰撞检查后发布给轨迹服务器执行和可视化。
```

## 三、展示前准备

如果在容器或 root 环境中打开 RViz，先在宿主机执行：

```bash
xhost +local:root
```

进入工程后编译并加载环境：

```bash
cd ~/workspace/sphere_robot
catkin_make
source devel/setup.bash
```

展示前建议先关闭其他 ROS launch，避免节点重名导致旧节点被杀掉。

## 四、RViz 颜色说明

展示时先解释颜色，后面观众才容易看懂：

- 洋红色点：A* ROLL 路径点
- 橙红色点：A* JUMP 路径点
- 蓝色连续线：最终发布的 B-spline 轨迹
- 橙红色连续线：被碰撞检查拒绝的候选轨迹
- 彩色实体块：真实障碍点云
- 淡蓝透明块：膨胀障碍或地图辅助显示
- 青绿色球：全局目标点

RViz 地面网格中，一个小方格代表 `1 m`。地图规划分辨率为 `0.1 m`。

## 五、演示 1：只看 A* 决策

目的：先把 ROLL / JUMP / 绕行决策讲清楚，不被 B-spline 平滑效果干扰。

运行：

```bash
roslaunch ego_planner bend_jump_test.launch astar_only:=true use_control:=false use_rviz:=true
```

观察重点：

- 低障碍处，洋红色 ROLL 点会直接通过，表示机器人可以滚越。
- 低墙处，路径会出现橙红色 JUMP 点，表示 A* 选择跳跃。
- 高障碍处，A* 不会强行跳跃，而是绕开障碍。
- JUMP 段不是直线，而是抛物线采样点。

讲解口径：

当前 A* 不再只把障碍看成“能走/不能走”，而是根据障碍高度和机器人能力分成三类：可滚越、可跳越、必须绕行。

## 六、演示 2：A* 转 B-spline 轨迹

目的：展示从离散路径到连续轨迹的转换效果。

运行：

```bash
roslaunch ego_planner bend_jump_test.launch astar_only:=false use_control:=false use_rviz:=true show_local_traj:=true show_whole_traj:=true planning_horizon:=7.5 enable_local_replan:=false enable_periodic_replan:=false optimize_direct_astar:=false
```

观察重点：

- 出现蓝色连续线，说明最终 B-spline 轨迹通过碰撞检查并发布。
- 蓝线整体应贴近 A* 路径点。
- JUMP 段蓝线应基本贴合橙红色抛物线点。
- 如果出现橙红色连续线，表示候选轨迹被拒绝，不是最终可执行轨迹。

讲解口径：

当前 direct A* 到 B-spline 的转换保留了 ROLL/JUMP 结构。ROLL 段做轻量平滑，JUMP 段尽量保持 A* 生成的抛物线，不再让旧 rebound optimizer 强行改变跳跃形状。

## 七、演示 3：中期固定场景一次性轨迹

目的：展示中期报告采用的最终口径：开局完整地图，固定终点，一次规划，不重规划。

运行：

```bash
roslaunch ego_planner midterm_global_demo.launch astar_only:=false use_control:=false use_rviz:=true show_local_traj:=true show_whole_traj:=true enable_local_replan:=false enable_periodic_replan:=false
```

说明：

- `midterm_global_demo.launch` 的终点默认是 `goal_x=0.0, goal_y=0.0, goal_z=0.0`，位于一次规划范围内。
- `sensing_horizon=22.0`、`local_update_range_x=22.0`，用于让整张展示地图在开局进入规划地图。
- `astar_pool_size_x/y/z=240/140/50`，用于支持这张大地图一次性 A* 搜索。
- `show_whole_traj:=true` 显示当前一次性生成并发布的 B-spline 采样轨迹。
- `enable_local_replan:=false` 是局部重规划总开关，保证执行过程中不切入 `REPLAN_TRAJ`。
- `enable_periodic_replan:=false` 关闭周期性重规划触发，作为后续工作预留接口。

讲解口径：

本实验不展示在线重规划能力，而是验证在已知静态地图中，规划器能否根据障碍语义一次性生成包含 ROLL、JUMP 和 detour 的固定轨迹。

## 八、演示 4：固定轨迹生成与跟踪效果验证

目的：在不修改控制器的前提下，验证规划得到的一条固定 B-spline 轨迹能否通过 `traj_server` 转换为位置命令，并由现有无人机仿真控制器进行跟踪。该实验用于证明“轨迹生成-命令发布-仿真执行”链路已经打通。

### 1. 实验定位

中期展示中假设机器人已知静态全局地图信息，重点展示一次规划得到的固定轨迹及其跟踪效果，不展示在线滚动重规划。当前阶段控制器暂时沿用 EGO-Planner 原有的无人机/点质量位置跟踪控制器，不把它包装成真实球形机器人动力学控制。

报告中建议表述为：

```text
当前阶段完成规划层固定轨迹输出与仿真执行链路验证，底层控制暂采用简化位置跟踪模型。在线重规划和球形机器人专用控制器将在后续工作中进一步研究。
```

### 2. 运行命令

用于固定轨迹跟踪验证时，需要打开控制器，但关闭周期重规划：

```bash
roslaunch ego_planner midterm_global_demo.launch astar_only:=false use_control:=true use_rviz:=true show_whole_traj:=true enable_local_replan:=false enable_periodic_replan:=false optimize_direct_astar:=false
```

运行后观察：

- `traj_server` 接收最终 B-spline 轨迹
- `/planning/pos_cmd` 持续输出期望位置、速度和加速度
- `/visual_slam/odom` 中机器人位置和速度随时间变化
- FSM 从 `GEN_NEW_TRAJ` 进入 `EXEC_TRAJ` 后保持执行，不反复进入 `REPLAN_TRAJ`

### 3. 需要记录的话题

建议用 rosbag 记录：

```bash
rosbag record /planning/pos_cmd /visual_slam/odom /grid_map/occupancy /grid_map/occupancy_inflate
```

核心曲线只需要前两个话题：

```text
/planning/pos_cmd：期望位置、期望速度、期望加速度
/visual_slam/odom：实际位置、实际速度
```

### 4. 推荐绘制曲线

第一组：期望位置与实际位置

```text
x_ref(t) vs x_odom(t)
y_ref(t) vs y_odom(t)
z_ref(t) vs z_odom(t)
```

作用：说明机器人是否沿规划轨迹方向运动，绕行和跳跃段是否在位置层面有响应。

第二组：期望速度与实际速度

```text
|v_ref(t)| vs |v_odom(t)|
```

其中：

```text
|v_ref| = sqrt(vx_ref^2 + vy_ref^2 + vz_ref^2)
|v_odom| = sqrt(vx_odom^2 + vy_odom^2 + vz_odom^2)
```

作用：验证仿真机器人是否能够跟随轨迹速度变化。

第三组：速度跟踪误差

```text
e_v(t) = |v_ref(t) - v_odom(t)|
```

也可以分轴画：

```text
e_vx = vx_ref - vx_odom
e_vy = vy_ref - vy_odom
e_vz = vz_ref - vz_odom
```

作用：给出可量化的跟踪效果，而不是只展示 RViz 截图。

第四组：跳跃段高度曲线

```text
z_ref(t), z_odom(t), obstacle_height, obstacle_height + jump_vertical_clearance
```

作用：说明 JUMP 段在轨迹上形成了高度变化，并且相对障碍顶部保留安全余量。

### 5. 推荐统计指标

可以在报告里放一个表格：

```text
平均位置误差 mean(|p_ref - p_odom|)
最大位置误差 max(|p_ref - p_odom|)
平均速度误差 mean(|v_ref - v_odom|)
最大速度误差 max(|v_ref - v_odom|)
RMS 速度误差 sqrt(mean(|v_ref - v_odom|^2))
```

这些指标用于说明当前固定规划轨迹在简化仿真控制器下具备基本可跟踪性。

### 6. 结果解释口径

如果跟踪效果较好，可以写：

```text
实际速度能够基本跟随期望速度变化，说明固定规划轨迹已经能够通过 traj_server 转换为连续位置命令，并驱动仿真机器人完成运动。
```

如果跳跃段或模式切换处误差较大，可以写：

```text
由于当前底层控制器仍采用简化位置跟踪模型，且尚未引入球形机器人真实滚跳动力学约束，模式切换和跳跃段存在一定跟踪误差。该问题将在后续控制器设计阶段进一步优化。
```

这部分实验的重点不是证明控制器已经完成，而是证明规划输出具备固定连续轨迹形式，并且已经接入仿真执行链路。

## 九、演示 5：答辩展示场景

目的：展示专门设计的稍大综合场景，包括滚越、跳跃、绕行、二次跳跃和窄通道视觉障碍。中期首选使用 `midterm_global_demo.launch`，它按“完整已知地图、一次规划到终点”的展示口径配置。

运行：

```bash
roslaunch ego_planner midterm_global_demo.launch use_control:=false use_rviz:=true show_local_traj:=true show_whole_traj:=true enable_local_replan:=false enable_periodic_replan:=false
```

只看 A*：

```bash
roslaunch ego_planner midterm_global_demo.launch astar_only:=true use_control:=false use_rviz:=true
```

展示顺序：

1. 低障碍滚越：说明 `roll_over_height` 的作用。
2. 第一段跳跃：说明 `max_jump_h`、`max_jump_d`、`jump_takeoff_clearance`、`jump_landing_clearance` 的作用。
3. 高障碍绕行：说明超过跳跃能力后不会强行跳跃。
4. 绕行后再次跳跃：说明路径可以包含多段 ROLL/JUMP 模式切换。

注意：

中期展示时使用 `midterm_global_demo.launch` 默认目标即可。若需要比 18 m 更长的路线，应继续增大 A* 搜索池并评估内存/耗时，或者后续再做分段轨迹；当前中期口径只展示这张已知地图上的一次性固定轨迹生成与跟踪。

## 十、关键参数讲解

展示时可以重点讲这些参数：

- `planner/roll_over_height`：低障碍滚越高度阈值。
- `planner/roll_over_penalty`：滚越低障碍的代价。
- `planner/max_jump_h`：可跳越障碍最大高度。
- `planner/max_jump_d`：单次跳跃最大水平距离。
- `planner/jump_takeoff_clearance`：起跳前安全距离。
- `planner/jump_landing_clearance`：落点后方安全距离。
- `planner/jump_vertical_clearance`：跳跃弧线越过障碍顶部时的高度余量。
- `planner/jump_penalty`：A* 中选择跳跃的额外代价。
- `planner/frontal_jump_cos`：限制跳跃方向与目标方向的一致性。
- `planner/use_inflate_for_jump`：跳跃可行性是否参考膨胀障碍。

建议讲法：

这些参数共同决定机器人什么时候滚、什么时候跳、什么时候绕。当前演示中，低于 `roll_over_height` 的障碍允许滚越；高于滚越阈值但低于 `max_jump_h` 的障碍可以尝试跳跃；超过跳跃能力的障碍会触发绕行。

## 十一、已完成工作总结

当前已经完成：

- 基于球形机器人运动特性的 ROLL / JUMP 混合 A*。
- 低障碍滚越逻辑。
- 跳跃起跳、落点和抛物线高度检查。
- detour 绕行逻辑，避免不可跳障碍被强行跳越。
- direct A* 到 B-spline 的连续轨迹转换。
- JUMP 段抛物线保形处理。
- 最终 B-spline 碰撞检查。
- RViz 中 A* 点、JUMP 点、B-spline 轨迹和失败候选轨迹的可视化。
- 周期重规划开关，中期演示固定关闭，用于展示一次性固定轨迹。
- 终端日志精简，减少无关 A* 调试刷屏。

## 十二、当前问题和后续计划

当前仍存在的问题：

- ROLL/JUMP 目前仍被拟合到单条 B-spline 中，极端场景可能出现形状偏差。
- 低障碍滚越段在 RViz 中仍按接地点显示，视觉上可能像“穿过”低障碍。
- 当前中期演示采用有限长度固定场景；如果把终点放得过远，仍需要重新配置搜索范围和节点池。
- 现有跟踪控制器是无人机/点质量位置控制器，不能代表真实球形机器人动力学控制。

后续计划：

- 将轨迹生成改为真正的分段结构：ROLL 段贴地 B-spline，JUMP 段独立抛物线。
- 对 ROLL_OVER 段做单独可视化，让滚越语义更清楚。
- 增加更长路线下的全局搜索池配置或真正分段轨迹框架。
- 后续设计球形机器人专用控制器，替换当前简化无人机位置跟踪控制器。

## 十三、推荐汇报话术顺序

可以按下面顺序讲：

1. 介绍问题：球形机器人既能滚又能跳，普通避障规划无法表达这种能力。
2. 介绍方法：在 A* 中加入 ROLL / JUMP / detour 三种语义。
3. 展示 A*：先用 `astar_only:=true` 展示离散决策。
4. 展示连续轨迹：用 `astar_only:=false` 展示 B-spline。
5. 解释颜色：洋红 ROLL、橙红 JUMP、蓝色最终轨迹。
6. 解释安全检查：最终轨迹发布前会检查 raw 障碍和 inflated 障碍。
7. 总结进展：已经完成混合 A*、跳跃检查、B-spline 转换和可视化。
8. 展望后续：做真正的分段轨迹、球形机器人专用控制器和更长路线实验。
